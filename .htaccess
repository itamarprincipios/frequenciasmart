Options -Indexes
RewriteEngine On

# Permite arquivos e diretórios reais (assets)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Redireciona tudo para index.php com a rota como parâmetro
RewriteRule ^(.*)$ index.php?rota=$1 [QSA,L]
